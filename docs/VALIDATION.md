# 验证记录

## 0.9.3 收藏同步修复 · 2026-09-17

收藏复用 Rust 字段级合并和歌曲删除墓碑；旧版本收藏一次性迁移，同步响应更新收藏页、计数及爱心状态，不改播放队列。新增浏览器回归覆盖 0.8 升级、0.9.2 已启用同步后的迁移、远端增删、请求途中编辑及失败重启恢复；Rust 用例验证两端独立收藏同 ID 不同平台歌曲后合并，以及取消收藏后旧快照不复活。

验证通过：79 项全量浏览器回归，加跑 9 项同步回归（包括后补的 0.9.2 升级用例）；43 项 Rust 测试通过、7 项联网测试按配置跳过；8 项 Node 与 5 项发行基础设施检查通过。源码及 Git 历史凭据扫描通过。

现场排查发现「应用程序」中实际运行的是 0.8.0，尚无同步能力，Mac 的 iCloud/Ting 目录无设备快照；旧版界面仍保留用户原有收藏与本机歌单。真实跨设备传输必须在两端更新并授权同一目录后验证，自动测试不能替代该步骤。


## 0.9.2 iCloud Drive 歌单同步 · 2026-09-17

76 项浏览器回归、42 项 Rust 离线测试、8 项 Node 测试、发布基础设施测试通过。macOS 原生书签/文件协调测试覆盖实际授权书签的解析、读取回写、相同内容不重复写入、无效书签与符号链接拒绝。iOS arm64 编译检查通过；源码候选与历史凭据扫描未发现泄露。

新增同步用例覆盖迁移一次、目录取消、接收改名、停用保留本地、请求未完成时继续编辑、失败后重启恢复，以及本机数据库丢失保护。真机 iCloud 文件夹授权及双端云传输需在安装后联调，不能以离线回归替代。范围与冲突策略见 [iCloud 同步说明](ICLOUD-SYNC.md)。

## 0.9.1 手机交互与工作流复审 · 2026-09-17

71 项浏览器回归全部通过，涵盖手机默认短信登录、冷却与取消、原像素二维码分享、Logo 重复点击、列表滚动、歌单缓存和退出账号竞态，并模拟 iOS 键盘只改变 VisualViewport 的情况；35 项 Rust 离线回归通过，7 项在线用例默认忽略。本轮另外显式运行网易云真实 QR / 音质只读回归，通过；没有发送真实短信或使用真人验证码。iOS arm64 编译检查通过。发现、修复与验证边界见 [工作流复审](WORKFLOW-REVIEW.md)。

手机验证码完成授权、系统分享活动最终保存的图片、QQ / 微信是否接受相册中的登录码，以及连续播放 / 后台行为仍需对应真机与平台验收。模拟接口和浏览器截图不作为这些操作已完成的证明。

## 0.8.0 稳定性与发布加固 · 2026-09-17

本节为当前结果；下方为按版本保留的历史记录，不能据旧结果推断当前产物的大小或依赖。

- `npm run build`、`npm run format:check`、Rust 格式检查通过。
- Playwright **35** 项、Node **8** 项、Rust **6** 项、QQ Python **18** 项、下载 Python **15** 项、发布基础设施 **10** 项通过，共 **92** 项；Rust 3 项外部服务测试默认忽略，本轮未执行。
- 覆盖移除历史歌曲、移除当前曲目后的曲末续播、切音质不重新入队、单曲循环与移除交互、登录恢复期间开关弹窗、损坏本机存储启动容错、5,000 首歌曲选中只更新两行。
- 同机 Chromium 性能探针：1,500 / 5,000 首的选中处理均为 0–0.1 ms（5 次样本，接近浏览器计时精度）。修复前为 33.0–35.8 / 361.6–375.8 ms。这不是系统 WebView 性能或内存占用承诺。
- 合成 24bit / 192kHz FLAC 经完整解码通过；截断 FLAC、截断 MP3、中段损坏 FLAC 被拒绝。校验声明字节数、MD5、实际解码时长、平台来源标签、严谨补源匹配及错误脱敏。
- 内置 CPython 3.12.14 与依赖在独立目录准备、导入通过。发布脚本检查源码提交和资源逐文件哈希，并在限制 PATH 的环境中检查 `.app` 内解释器及原生依赖，不依赖 Homebrew Python。
- 公共二维码素材全部使用合成载荷。QQ 470px JPEG 在两种窗口大小下的最终截图实际解码与载荷相同、适配后 PNG 像素一致。未重新操作私人账号扫码或云端歌单写入。
- npm 与 Python 漏洞扫描及 Cargo 目标依赖核查结果见 [安全审查](SECURITY-AUDIT.md)。源码和历史须在提交发布时再过凭据扫描。

已用隔离应用副本验证原生静态页面加载、网易云真实搜索、480→800→480 的右侧歌词展开 / 收回，以及本地静音 WAV 导入、双击播放和曲末结束。原生验收发现旧 QQ 设备缓存与官方 SDK 的结构差异，已通过只读兼容层修复，并验证原缓存字节保持不变。随后更新后的原生副本已确认网易云、QQ 既有登录同时恢复成功。最终发布包和 GitHub CI 的结果随发布记录补充；浏览器模拟窗口尺寸测试不等同于所有 macOS 全屏 / 多屏组合均已验证。仅发布 macOS 15+ Apple Silicon；Linux、Windows、iOS、Android 未交付。

## 初版 · 2026-09-16

日期：2026-09-16。平台：本机 macOS / Apple Silicon。

- TypeScript 类型检查、Vite 生产构建通过。
- Rust 单元测试：WEAPI 双层 AES 往返和 RSA 输出长度通过。
- Rust 真实网络集成测试通过：搜索「陈绮贞」返回 274 条结果，获得首曲播放地址；音频实际请求成功并读取到音频字节；歌词返回 1526 字节。
- JavaScript 单元测试 3 项通过：多时间戳 LRC 与偏移、未知时长格式化、队列去重。
- Playwright 交互测试 2 项通过：收藏跨刷新保存、队列去重、搜索请求竞态、不可播放错误；浏览器断网后的实际 WAV 导入 / 播放 / 进度拖动 / 暂停 / 切歌 / 播放结束。云端 UI 测试使用 mock，真实接口单独由 Rust 测试覆盖。
- Tauri release 构建通过，生成 macOS ARM64 `.app`，约 14 MB。
- 实际 macOS 应用窗口验证通过：`tauri://localhost` 内置资源加载；网易云搜索展示 30 条 / 总计 274 条；播放「旅行的意义」返回游客 30 秒试听音频；进度推进到 19 秒，封面和歌词加载，暂停成功。测试以静音进行，结束后恢复应用音量状态，保持暂停。

未验证：Windows / Linux / 手机平台、账号登录、完整会员歌曲权限、所有音频格式、Apple 签名公证。当前应用为个人使用 Demo，游客歌曲权限以接口实际返回为准。

## 0.2 更新验证

日期：2026-09-16。

- 已接入扫码登录 / 退出、macOS 钥匙串会话保存与恢复、个人歌单 / 歌曲分页、8 档音质参数与实际音质展示。
- 真实接口：二维码获取及 801 等待扫码状态通过；取消后旧 challenge 不再有效。
- 真实音质请求：游客账号请求 exhigh / lossless / hires，接口实际返回 standard + trial；前端会显示实际权限结果，不把试听标记成无损。
- 真实公开歌单：131 首的歌单，首批与第二批读取成功，跨页歌曲不重复。
- Playwright：7 项交互测试通过，覆盖登录 801/802/803、过期 / 关闭、模拟私有歌单读取、完整歌单队列、音质切换保留进度与暂停状态、退出清理、本地离线播放、收藏与搜索竞态；新增延迟歌词期间切换音质、暂停中断 play promise 的回归。
- QR：对实际显示的二维码截图解码，内容与完整登录 URL 逐字一致；截图尺寸和像素与原始 PNG 完全一致。生产代码每次生成二维码后也会解码 PNG，自检失败不显示。
- 闪烁回归：播放 / 暂停、音量、收藏之后，导航 SVG 和已有歌曲行仍是同一 DOM 节点；歌词高亮前后行高一致。

原生应用实测：用户本人已扫码并确认授权，应用展示 224 个个人创建 / 收藏歌单；读取歌单歌曲并播放完整无损 FLAC，界面显示实际码率（观察到 1684 kbps 和 1477 kbps）。请求超清母带时服务返回无损，界面正确提示实际音质。重新打开应用后恢复账号并继续读取歌单，无需再次扫码。

未验证：所有账号 / 歌曲的权限、实际 Hi-Res / 母带音源、其他平台及签名公证。实际退出登录未操作，以免中断用户会话；退出流程由 mock 自动化覆盖。最后的歌词竞态和暂停中断修复通过浏览器回归，已重新构建交付，正在运行的旧窗口需重开后生效。

## 0.2.1 字体调整

- 默认 1180×800 与最小 880×620 窗口截图检查通过，主要面板和播放控制栏无横向溢出。
- 复用现有交互回归：DOM 稳定性、固定歌词行高、登录 / 歌单 / 音质 / 退出流程通过。
- 登录二维码截图解码与完整 URL 一致，且与原始 PNG 尺寸及像素一致。

## 0.3 小窗口与随机播放

- 480×720 默认窗口、400×560 最小窗口检查，无横向溢出；歌词展开时控件可用、列表独立滚动。
- JavaScript 单元测试 4 项通过；新增 Fisher–Yates 队列排除当前曲目、去重、单曲 / 空队列边界。
- 浏览器交互测试 9 项通过：原有账号、音质、离线播放回归；新增随机一轮不重复、上一首历史、历史前进、结束自动随机下一首、模式跨刷新保存、单曲循环。
- 默认和最小窗口的登录二维码均完成实际截图解码，逐字匹配完整 URL，并与原始 PNG 逐像素一致。小窗口二维码按容器宽度使用整数倍模块尺寸，不缩放原图。
- 本次修改未涉及 Rust 网易云接口；账号权限仍以服务实际返回为准。

- 原生 macOS v0.3.0 已启动，480px 宽窗口截图检查通过；钥匙串账号恢复成功，用户已在新版打开自己的歌单并选择歌曲。应用完成本地 ad-hoc 签名校验（非 Developer ID 公证）。

## 0.3.1 完整歌单队列

- 修复点单曲时只将已加载的 100 首加入队列；现在立即播放并后台补齐所有页面，统一点击 / 键盘 / 主播放键 / 播放全部入口。
- 修复 localStorage 恢复时的 1,000 首截断。
- Playwright 四项相关回归通过：登录与完整歌单队列、随机与历史、1,205 首队列跨页面后台补全及刷新恢复、旧分页结果不能覆盖新播放列表。大歌单数据为 mock，用于验证逻辑，不代表对用户私人歌单做了 1,205 首实测。

## 0.4 原音乐库下载流程

- Rust 单元测试 2 项通过。下载助手 Python 测试 3 项通过：拒绝错误版本匹配 / 试听回退、QQ 补源 mock、真实 MP3 标签与 LRC / 来源文件写入、重复下载不覆盖。
- 三项浏览器回归通过：下载实际来源 / 音质展示、下载不打断播放、重复点击禁用；另覆盖账号 / 音质流程和最小窗口二维码逐像素检查。
- 真实下载：使用原无损音乐库的网易云登录态，下载歌曲 3378909144，42,511,355 字节。ffprobe 检查 FLAC 24bit / 48000Hz，完整 202.63 秒，标题 / 歌手 / 专辑和嵌入 JPEG 封面存在；LRC 与来源 JSON 已写入测试输出。
- QQ 实测：旧登录文件存在，搜索和精确匹配成功，但没有取得该测试歌曲的下载地址。QQ 下载仅 mock 回归通过，当前实际账号可用性仍未通过。
- Python 下载助手仅在下载时启动；播放架构不变。现有音乐库与登录文件未修改。

## 0.4.1 双击播放（2026-09-17）

TypeScript / Vite 构建通过；浏览器 13 项回归通过。新增验证：歌名或歌曲行单击只选中、不调用音源接口；双击仅发起一次播放；当前播放不因单击其他歌曲中断；回车可播放；双击收藏不会触发播放。原有登录二维码逐像素和解码、分页完整队列、随机播放、本地离线及下载交互回归通过。QQ 双账号和跨平台歌单本次仅完成可行性分析，尚未实现。

## 0.5 双平台（2026-09-17）

- TypeScript / Vite、Rust 编译、macOS app 打包通过；17 项浏览器交互回归、4 项模型测试通过。新增双登录、两平台同 ID 歌曲与歌单、混合收藏和队列、退出单平台保留另一平台播放、独立音质映射、单平台歌单失败隔离测试。
- QQ/微信真实二维码获取、QQ 未扫码轮询（801）通过；QQ 实际搜索可返回 Espresso (On Vacation Version) / Sabrina Carpenter。已修正搜索 Desktop 方法和参数，Android 默认路径曾遇限流。
- QQ 免费曲目 On Vacation / Aimee Allen：真实最高可用请求降至标准；读取完整文件并验证 200.47 秒、128000 bps、44100 Hz、3208928 bytes；歌词取得成功。会员高音质和个人歌单需要用户扫码后进一步实测。
- 原始 QQ PNG 111×111 在 400×560 窗口按整数 2 倍显示。最终截图逐像素检查每个原像素对应 2×2 像素块，实际解码并与原图完整内容核对一致；没有生成或改写二维码码点。此验证不代表用户手机扫码授权已完成。
- QQ 凭据独立钥匙串项 qq-session，子进程经 stdin 接收，前端不收到 credentials / refresh token / qrsig。账号操作使用版本号拒绝已取消的旧响应；迟到的旧二维码轮询不会清除新二维码。并发搜索和播放不持有网络期间的账号状态锁。
- 打包目录中的 Python 助手已运行真实搜索并验证依赖导入；无常驻服务。保留当前正在播放的旧进程，新版退出重开生效。

## 0.5.1 微信二维码（2026-09-17）

根因：verifyOriginalQr 在解码前把 470px 的微信原图判定为超宽并拒绝。修复为保留全图、2 倍整数缩小到 235px、补 8px 白边，得到 251px 无损 PNG；不重新生成码点。6 项 QQ 集成回归通过，其中新增真实 470px 微信 JPEG 在 400×560 / 480×720 下的截图解码、原图内容逐字一致、截图与输出 PNG 像素一致、刷新按钮可见测试。TypeScript / Vite 与 release 打包通过。未声称用户手机实际扫码登录已验证。


## 0.6.0 歌单管理、跨平台匹配、QQ 会员播放（2026-09-17）

- 25 项浏览器交互测试通过（24 项完整回归 + 新增后台队列补全期间编辑的专项回归），4 项模型单元测试、3 项 Rust 单元测试通过；3 项依赖在线服务的 Rust 测试未在本轮运行。8 项 Python 离线回归通过。
- 新增本机混合歌单创建、持久化、增删、排序、重命名、删除；两平台云歌单的加入/移除路由、网易云排序路由、只读歌单限制、写入失败保留原列表；QQ 本机排序覆盖 137 首跨页并验证重开保存和恢复原序；网易云排序纯函数覆盖 1307 个 ID 完整保留。跨平台匹配明确确认后仅发送目标平台曲目 ID。
- 保留正在播放的用户应用进程，未为测试修改实际云端歌单。云写入验证为离线模拟和后端授权/失败处理回归，未进行真实账号歌单增删验收。
- 真实 QQ 微信登录凭据从钥匙串读取，仅在进程内使用，不写入测试文件或日志。修复前《晴天》无损/320k/128k 均返回 104003；仅补齐 authst/tmeLoginType/UIN 后三档均返回 result=0 和非空 purl。修复已应用到 WEB/DESKTOP 的凭据公参，也覆盖个人歌单写入。
- 用户指定的周杰伦《红尘客栈》（QQ id 5177680）：FLAC/320k/128k 实际地址均返回 HTTP 206 音频数据，FLAC magic 为 fLaC，MP3 为 ID3。生产 bridge 无损返回 lossless、最高可用返回 master；隔离的 macOS WKWebView 静音播放探针实际解码并前进到 0.49/0.42 秒，读取完整时长 274.088/274.069 秒。不是仅检查拿到 URL；未声称完整听完全部音频，也不代表所有歌曲权限都已测试。
- QQ/微信二维码的默认及最小窗口最终截图解码回归通过，保持与原始内容一致。构建、ad-hoc 签名与安装校验见发布检查。


## 0.7.0 分类、主题、窗口和 QQ 下载（2026-09-17）

- 27 项完整浏览器回归通过；随后新增 QQ 下载专项，与原网易云下载专项同时通过。包括 100 个网易云歌单与 QQ 分栏、最近播放持久化/平台内前排/失败不记、11 套主题全局覆盖及文字对比度 >4.5、400×560 无横向溢出、选中行无粗边、原有二维码最终截图像素和解码验证。
- 4 项模型测试、3 项 Rust 单元测试、8 项 QQ bridge 离线测试、5 项下载助手测试通过。3 项外部服务 Rust 测试继续保持 ignored。
- macOS 原生预览窗口截图确认 Overlay 标题栏与内容融为一体，顶部有完整主题/账号按钮；用户已在此预览窗口播放 QQ 母带，未中断其播放进程。标题栏保留系统关闭/最小化/全屏控件，拖动权限显式限定主窗口。
- 真实 QQ 直接下载《红尘客栈》（5177680）：完整母带 FLAC，172,573,024 bytes，24bit/192000Hz，约274秒；封面、标题、歌手、专辑、歌词齐全，无 warnings，输出至 Downloads/Ting。签名 URL 仅在内存和进程 stdin 传输，未进入来源 JSON。
- 最近播放只能从本版起记录，旧版本没有采集歌单播放记录。QQ 排序仍明确只保存在本机；云歌单写入仍未操作用户私人歌单进行验收。


## 2026-09-17 · 源码审查与文档整理

项目主目录已改为 `ting-music`。完整使用及开发说明更新至根目录 README，发现的问题见 [REVIEW.md](REVIEW.md)。本次前端构建与现有 49 项离线 / 模拟回归通过，Rust 3 项联网测试未执行；额外探针复现 3 项队列 / 账号状态缺陷，并测得大列表选中开销。独立空资源目录的 Python 依赖准备与 QQ 补丁校验通过。

本次未修改业务代码、未升级运行中的应用、未执行私人云端歌单写入。历史记录中的版本功能说明不等同于当前安装包已更新。

## 2026-09-17 · 0.9.0 Rust 迁移与 iOS 真机版

QQ 协议请求、扫码轮询、会员音源解析与下载校验已迁至 Rust。移除打包 Python 运行时、QQ Python bridge、miniaudio/Mutagen 及运行时准备脚本，改用 reqwest、Symphonia 与 Lofty。旧的 Python 用例已迁为 native Rust 回归，测试总数不能与旧版直接比较。

已完成的检查：

- Rust 离线回归 29 项通过，Node 模型 8 项、发布基础设施 5 项通过。覆盖会员参数、32 字符 GUID、真实 media MID、音质优先级、歌单所有权与歌曲类型、凭据隔离、精确录音匹配、限流/网络错误、校验和、截断/损坏音频、192 kHz / 24-bit FLAC、MP3、标签与禁止覆盖。
- QQ 在线只读搜索、歌曲详情、歌词、QQ/微信二维码获取及等待状态通过；已有会员账号的《红尘客栈》无损解析与歌单读取通过。网易云 3 项已有在线回归通过。
- 两个平台真实下载端到端均通过：网易云 ChiliChill 歌曲、QQ 会员《红尘客栈》FLAC，覆盖当前账号解析音源、下载、完整解码、标签、来源 sidecar 与临时文件清理；使用临时目录，完成即删除测试音乐。
- 完整生产前端浏览器回归 37 项通过。第一次开发服务器测试有 1 项在页面重载后失败，单项复测通过；随后改用独立端口的构建产物预览服务，完整 37 项通过，避免 HMR 与开发会话影响回归。默认 Playwright 配置已固定采用该流程。
- macOS 原生应用构建成功，浏览器与原生窗口检查顶栏布局；标题居中、去除重复 Logo，给红绿灯与账号工具分别留空间。新增 400px Mac 标题布局与 iPhone 全屏歌词面板检查。
- iOS 16+ arm64 开发包成功构建、导出并安装到连接的 iPhone。签名校验通过、描述文件包含目标设备。Xcode 27 的 SwiftRs C 导出问题通过可追踪的本地源码补丁处理，详情见 `src-tauri/compat/swift-rs/TING-PATCH.md`。
- npm audit：0 已知漏洞。Git 候选文件与完整历史凭据扫描通过。Rust 锁文件共 492 个第三方包经 OSV 核查：glib 0.18.5 的已知问题仅在未发布的 Linux 依赖树；其余为 paste/proc-macro-error/unic 系列的上游停止维护告警。不能将此称为整个 Rust 依赖树“零告警”。

首次 iOS 启动需要用户在系统设置中信任开发者身份。用户完成信任后发现闪退；真机崩溃日志定位到 iOS 27 Scene 生命周期检查，已增加 Scene manifest 并启用 Tao 内置 Scene delegate。启用后还复现了 Tao 0.35.3 返回 Scene configuration 时的 use-after-free，已固定官方修复提交并声明静态 Scene configuration；修复版已安装并通过启动后进程存活检查，用户随后已进入界面反馈交互问题。手机上的完整扫码确认、连续前台播放及锁屏/后台行为尚未完成验收；首版不承诺后台播放。在线回归没有增删用户云端歌单。开发签名仅适用于授权设备且会过期，不能用于普通用户分发。

### 0.9.0 平台交互适配

iOS / Android 歌曲轻点播放、同曲重复点击不重启、暂停后轻点继续；桌面保留单击选择、双击与回车播放。共享平台判断覆盖 iPadOS 桌面 UA；账号凭据文案按实际后端能力显示，扫码提示按平台与 QQ / 微信方式变化。手机歌词面板不调用桌面窗口 API，下载位置提示使用 iOS 文件 App，搜索提交收起键盘，触控按钮至少 44px。

新增 6 项移动端回归，覆盖 iPhone 393px、320px 窄屏、Android 360px 与 iPad 820px，含重复点击、歌曲操作不误触播放、保存位置、恢复账号、歌词和键盘焦点。微信二维码最终截图实际解码与内容核对、输出 PNG 像素一致性通过。完整浏览器套件现为 43 项；平台文案更新导致两处旧断言失败，更新断言后的专项复测通过。Node 8 项、发布基础设施 5 项、格式检查通过。

修复新检出仓库在 Rust 测试前缺少 resources/build 的初始化问题；在移走本地生成目录后重新执行 prepare-desktop，成功创建资源清单。源码候选文件和历史凭据扫描通过。Android / iPad 覆盖为浏览器设备模拟，不能代替对应真机验收。本次交互修正版的 iPhone 安装须等待设备恢复连接；不将前一构建的启动检查计为本版完整播放验收。
