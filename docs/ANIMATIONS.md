# 0.9.6 动画与界面稳定性

本轮保持原有紧凑布局和桌面双击／手机轻点播放方式，统一状态切换，并优先消除内容重建造成的闪烁。公开版本为 **0.9.6**，iOS 内部构建号为 **90601**；后续普通修复继续沿用该公开版本。

## 交互变化

| 场景                             | 行为                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 搜索、歌单、收藏、本地、队列切换 | 同级横切：按导航顺序，向右切内容从右侧进入、向左切从左侧进入，导航标记与内容同向同时起止；进入歌单详情为推入，返回为退出，三种方向的位移幅度与时长各不相同；重复点当前页面不重建内容、不重置滚动                                                                                                      |
| 歌单刷新和编辑后的刷新           | 保留已有卡片、歌曲行及封面；结果到达后只更新有变化的内容；请求失败保留已有内容                                                                                                                                                                                                                        |
| 列表内容到达、加载更多           | 只有本次新增的行做入场，最多 8 行、逐个延迟 22ms；被保留的行和视口外的行一帧都不动；视图切换时列表行不再单独入场                                                                                                                                                                                      |
| 手机左缘返回手势                 | 未过阈值回弹原位；过阈值后内容继续向右走完并淡出，上一层随即从左侧补位                                                                                                                                                                                                                                |
| 搜索新关键词或平台               | 保留上一批结果直至新结果到达；新请求失败后不允许把另一查询的分页混入旧结果                                                                                                                                                                                                                            |
| 播放状态                         | 正在播放时封面后方浮出一层由当前封面模糊而成的环境光晕，随音乐缓慢起伏；标题旁的三柱均衡器跳动。暂停时光晕淡出、柱子放平变灰；缓冲时柱子静止、以短周期闪烁（不假装在播）；播放失败为红色静止柱；无歌曲时隐藏。封面本身从不缩放或位移，光晕与柱子只动画 `opacity` / `transform`，窗口实时缩放不受影响。列表当前播放行的标识与之同步。状态以 `HTMLAudioElement` 的 `playing` / `pause` / `waiting` / `stalled` / `ended` / `error` 事件为准 |
| 切歌                             | 封面与标题同时、同方向替换：下一首自右侧进入，上一首自左侧；从列表点选为无方向的原地交叉淡入。下一张封面完成下载与解码后淡入；快速切换只接收最新请求，连续淡入不强制跳到中间帧                                                                                                                        |
| 歌词加载                         | 保留旧歌词的画面并暂时禁止点击，等当前歌曲歌词就绪后替换；旧请求不能覆盖新歌词                                                                                                                                                                                                                        |
| 歌词展开                         | 面板始终自右缘进出：桌面在常规流中滑入 28px 并协调原生窗口宽度，窄窗口与手机的固定抽屉推入自身整幅；支持尚未结束时立即反向关闭／打开                                                                                                                                                                  |
| 歌词跟随、切换音质               | 唱过、未唱、临近与当前四档透明度各不相同；当前行到达快、离开留余韵，只用透明度与小幅水平位移，字号、字重与行高始终不变；同歌切音质保留阅读位置                                                                                                                                                        |
| 账号、主题、歌单操作、同步设置   | 手机底部面板自下缘推入自身整幅、不越过零点；桌面以缩放为主浮起；关闭更快并改用加速曲线。快速重新打开不会被旧的关闭动画关掉；保留原生焦点约束                                                                                                                                                          |
| 歌单操作向导换屏                 | 每一步都是深入，新屏自右侧进入，与弹窗自身的纵向进出区分开                                                                                                                                                                                                                                            |
| 手机输入                         | 输入开始时结束弹窗位移动画，交给键盘避让，避免输入框被动画推到键盘下方                                                                                                                                                                                                                                |
| 登录二维码                       | 原图解码校验不变；等外层动画结束后再显示和对齐，保持二维码尺寸与实际像素                                                                                                                                                                                                                              |
| 主题                             | 同明暗配色平滑插值；跨深浅色同步切换文字和背景，避免中间态文字失去对比度                                                                                                                                                                                                                              |
| 播放按钮、收藏、提示消息         | 短促反馈；音量和播放进度更新不会反复替换图标或封面                                                                                                                                                                                                                                                    |

## 实现约束

- `src/motion.ts` 管理可取消的有限时长动画，结束后释放效果与引用。弹窗仍使用原生 `showModal()` / `close()`，保留焦点恢复和已有忙碌保护。
- `src/cover.ts` 在屏幕外解码图片，最多显示两层封面及等待中的最新一张，失败后允许重新请求相同 URL。
- `src/lyrics.ts` 将原生窗口调整串行执行；过期的过渡结果不能改变当前开合状态。
- `src/playback-state.ts` 只订阅音频事件并把结果写在 `body[data-playback]`；`src/playback-motion.ts` 据此启停封面呼吸，停止时读取当前帧再动回 identity，终态永远无残留 transform。
- 所有时长与曲线取自 `src/motion.css` 的 `:root` token（`--t-1`…`--t-5`、`--e-enter` / `--e-exit` / `--e-move` / `--e-breath`），`src/motion.ts` 的 `MOTION` 是同一份数值的 JS 镜像；进入与退出不对称，退出比配对的进入低一档时长并改用加速曲线。
- 播放按钮图标保持**意图态**（点了立刻变），封面与列表标识保持**事实态**（等真实 `playing` 事件）。两者分离，互不迁就。
- 持续动效只用合成层的 transform/opacity，窗口不可见时暂停，`prefers-reduced-motion` 下退化为静态但仍可辨的颜色与尺寸标识。
- 不逐行启动成百上千个动画，不对播放进度持续重绘，不使用全局 `transition: all`，不引入新的运行时依赖。
- 遵循系统 `prefers-reduced-motion`，运行中开启「减少动态效果」也会结束当前动画；所有功能继续可用。

## 验证

```sh
npm run build
npm test
npx playwright test
npm run check:release
python3 -B scripts/scan-secrets.py --history
```

`tests/motion.spec.ts` 覆盖稳定 DOM、刷新时保留内容、连续导航、延迟及乱序图片／歌词、弹窗反向切换和动态效果偏好的运行时变化。既有测试继续覆盖双账号登录、二维码最终像素及解码、歌单增删排序、播放队列、音质、系统媒体、离线文件、同步和手机键盘／缩放。

浏览器回归使用模拟平台接口，并不代表真实网易云／QQ 服务或所有原生设备均已验收。原生构建和装机状态以对应交付说明为准。
